# Shell packages from COPR. Installed in every profile, including --minimal,
# because the prompt is part of the base setup rather than an extra.
starship    # cross-shell prompt, configured by ONE toml file (atim/starship)
