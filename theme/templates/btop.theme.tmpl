# GENERATED by theme/apply-theme.py — do not edit.
# Flavour: {{flavour}} / accent: {{accent_name}}
#
# Yes, even btop. A monitor left in its default colours is exactly the sort of
# detail that makes a "fully themed" desktop look half finished in a screenshot.

theme[main_bg]="{{base}}"
theme[main_fg]="{{text}}"
theme[title]="{{text}}"
theme[hi_fg]="{{accent}}"
theme[selected_bg]="{{surface1}}"
theme[selected_fg]="{{accent}}"
theme[inactive_fg]="{{overlay1}}"
theme[graph_text]="{{subtext0}}"
theme[meter_bg]="{{surface1}}"
theme[proc_misc]="{{lavender}}"

theme[cpu_box]="{{accent}}"
theme[mem_box]="{{green}}"
theme[net_box]="{{maroon}}"
theme[proc_box]="{{blue}}"
theme[div_line]="{{overlay0}}"

theme[temp_start]="{{green}}"
theme[temp_mid]="{{yellow}}"
theme[temp_end]="{{red}}"

theme[cpu_start]="{{teal}}"
theme[cpu_mid]="{{sapphire}}"
theme[cpu_end]="{{lavender}}"

theme[free_start]="{{peach}}"
theme[free_mid]="{{maroon}}"
theme[free_end]="{{red}}"

theme[cached_start]="{{sapphire}}"
theme[cached_mid]="{{blue}}"
theme[cached_end]="{{lavender}}"

theme[available_start]="{{yellow}}"
theme[available_mid]="{{peach}}"
theme[available_end]="{{maroon}}"

theme[used_start]="{{green}}"
theme[used_mid]="{{teal}}"
theme[used_end]="{{sky}}"

theme[download_start]="{{peach}}"
theme[download_mid]="{{maroon}}"
theme[download_end]="{{red}}"

theme[upload_start]="{{green}}"
theme[upload_mid]="{{teal}}"
theme[upload_end]="{{sky}}"

theme[process_start]="{{sky}}"
theme[process_mid]="{{lavender}}"
theme[process_end]="{{mauve}}"
