// GENERATED by theme/apply-theme.py — do not edit.
// Flavour: {{flavour}} / accent: {{accent_name}}
{
    "$schema": "https://github.com/fastfetch-cli/fastfetch/raw/dev/doc/json_schema.json",
    "logo": {
        "type": "small",
        "padding": { "top": 1, "right": 3 }
    },
    "display": {
        "separator": "  ",
        "key": { "width": 12 },
        "color": { "keys": "{{accent}}", "title": "{{accent}}" }
    },
    "modules": [
        "break",
        { "type": "title",  "key": "󰣇", "keyColor": "{{accent}}" },
        { "type": "custom", "format": "───────────────────────────────" },
        { "type": "os",       "key": "󰍹 os",     "keyColor": "{{mauve}}" },
        { "type": "kernel",   "key": "󰌢 kernel", "keyColor": "{{blue}}" },
        { "type": "wm",       "key": "󱂬 wm",     "keyColor": "{{sapphire}}" },
        { "type": "terminal", "key": " term",   "keyColor": "{{teal}}" },
        { "type": "shell",    "key": " shell",  "keyColor": "{{green}}" },
        { "type": "packages", "key": "󰏖 pkgs",   "keyColor": "{{yellow}}" },
        { "type": "uptime",   "key": "󰅐 up",     "keyColor": "{{peach}}" },
        { "type": "cpu",      "key": "󰻠 cpu",    "keyColor": "{{maroon}}" },
        { "type": "memory",   "key": "󰍛 mem",    "keyColor": "{{red}}" },
        { "type": "disk",     "key": "󰋊 disk",   "keyColor": "{{pink}}" },
        { "type": "custom", "format": "───────────────────────────────" },
        {
            "type": "colors",
            "paddingLeft": 2,
            "symbol": "circle"
        },
        "break"
    ]
}
