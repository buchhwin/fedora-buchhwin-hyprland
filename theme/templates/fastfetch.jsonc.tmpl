// GENERATED by theme/apply-theme.py — do not edit.
// Flavour: {{flavour}} / accent: {{accent_name}}
{
    "$schema": "https://github.com/fastfetch-cli/fastfetch/raw/dev/doc/json_schema.json",
    "logo": {
        "type": "small",
        "padding": { "top": 1, "right": 3 }
    },
    "display": {
        "separator": "  ",
        "key": { "width": 12 },
        "color": { "keys": "{{accent.hex}}", "title": "{{accent.hex}}" }
    },
    "modules": [
        "break",
        { "type": "title",  "key": "󰣇", "keyColor": "{{accent.hex}}" },
        { "type": "custom", "format": "───────────────────────────────" },
        { "type": "os",       "key": "󰍹 os",     "keyColor": "{{mauve.hex}}" },
        { "type": "kernel",   "key": "󰌢 kernel", "keyColor": "{{blue.hex}}" },
        { "type": "wm",       "key": "󱂬 wm",     "keyColor": "{{sapphire.hex}}" },
        { "type": "terminal", "key": " term",   "keyColor": "{{teal.hex}}" },
        { "type": "shell",    "key": " shell",  "keyColor": "{{green.hex}}" },
        { "type": "packages", "key": "󰏖 pkgs",   "keyColor": "{{yellow.hex}}" },
        { "type": "uptime",   "key": "󰅐 up",     "keyColor": "{{peach.hex}}" },
        { "type": "cpu",      "key": "󰻠 cpu",    "keyColor": "{{maroon.hex}}" },
        { "type": "memory",   "key": "󰍛 mem",    "keyColor": "{{red.hex}}" },
        { "type": "disk",     "key": "󰋊 disk",   "keyColor": "{{pink.hex}}" },
        { "type": "custom", "format": "───────────────────────────────" },
        {
            "type": "colors",
            "paddingLeft": 2,
            "symbol": "circle"
        },
        "break"
    ]
}
