# GENERATED by theme/apply-theme.py - do not edit. {{flavour}}/{{accent_name}}
#
# Chromium reads its frame colour from a Local State key, not from a config
# file we can template directly — so apply-theme.py merges these two values
# into ~/.config/BraveSoftware/Brave-Browser/Local State. This file is the
# source of truth for what they should be, and exists so the value is visible
# and reviewable rather than buried in Python.
#
# The dark/light preference goes through the GTK setting, which Brave follows
# on Wayland; only the frame is set here.
frame={{crust.hex}}
toolbar={{mantle.hex}}
